# --- SonarCloud - AuthPackage (Swift) ---
sonar.organization=ciscode
sonar.projectKey=CISCODEAPPS_pkg-ios-auth
sonar.projectName=pkg-ios-auth

# Source layout for Swift Package Manager
sonar.sources=Sources
sonar.tests=Tests
sonar.inclusions=Sources/**
sonar.test.inclusions=Tests/**

# Use the LCOV we already generate in the pipeline
sonar.swift.coverage.reportPaths=coverage.swift.txt

# Optional: ignore generated or example code
# sonar.exclusions=Sources/**/Generated/**

# (Auto-detection handles language; no need to set sonar.language)
